# Text-summarization-T5-Encoder-Decoder
A Python-based text summarization tool that uses a T5 transformer model for AI-powered summaries, with a built-in fallback extractive summarizer when ML dependencies are unavailable.
